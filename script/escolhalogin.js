function login(tipo) {
    if (tipo === 'cooperado') {
      window.location.href = "login.html"; // Substitua com o caminho real
    } else if (tipo === 'empresa') {
      window.location.href = "loginemp.html"; // Substitua com o caminho real
    }
  }
  